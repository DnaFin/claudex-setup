# Case Study: prediction-bot — Audit (Claude Code platform)

**Date:** 2026-04-05
**Repository:** prediction-bot (C:\Users\naorp\prediction-bot)
**Platform audited:** claude
**Nerviq version:** 1.0.0

---

## Audit Result

| Metric | Value |
|--------|-------|
| **Score** | **23 / 100** |
| Checks passed | 13 |
| Checks failed | 64 |
| Grade | D |

---

## Interpretation

prediction-bot is in an early-stage configuration state. The presence of
some passing checks suggests the project has a basic structure, but lacks
the AI agent configuration depth needed for safe, effective AI-assisted
development. Two critical security checks are failing.

### Critical gaps

1. **No verification criteria in CLAUDE.md** — Add test/lint/build commands
   to CLAUDE.md so Claude can verify its own work after each change.
2. **.gitignore doesn't block .env** — Add `.env` to `.gitignore` immediately.
   This is a security risk with any API-key dependent bot.
3. **No secrets protection** — Configure `permissions.deny` rules to block
   access to credentials files.

### Security note

For a prediction bot that likely handles API keys, financial data, or external
service credentials, secrets protection should be addressed immediately before
any further AI-assisted development.

### Quick-win improvement path

```bash
cd C:\Users\naorp\prediction-bot
npx @nerviq/cli setup --auto   # generates safe baseline
# Then manually edit CLAUDE.md with domain knowledge
```

---

## Before / After Projection

| Phase | Score | Action |
|-------|-------|--------|
| Current | 23 | Minimal config, 2 critical security gaps |
| After `nerviq setup --auto` | ~45 | Baseline + security checks fixed |
| After full implementation | ~70 | Domain rules + verification |

*Generated by nerviq audit --platform claude --json*
