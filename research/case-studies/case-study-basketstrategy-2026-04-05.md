# Case Study: basketstrategy — Audit (Claude Code platform)

**Date:** 2026-04-05
**Repository:** basketstrategy (C:\Users\naorp\basketstrategy)
**Platform audited:** claude
**Nerviq version:** 1.0.0

---

## Audit Result

| Metric | Value |
|--------|-------|
| **Score** | **10 / 100** |
| Checks passed | 5 |
| Checks failed | 73 |
| Grade | D |

---

## Interpretation

This repository has minimal AI agent configuration — a common starting point
for projects that haven't yet adopted AI-assisted development conventions.
The low score reflects absence of configuration, not broken configuration.
This represents the highest-value improvement opportunity in this batch.

### Critical gaps

1. **No CLAUDE.md** — Create `CLAUDE.md` with project-specific instructions,
   build commands, and coding conventions.
2. **No verification criteria** — Add test/lint/build commands to CLAUDE.md
   so Claude can verify its own work.
3. **.gitignore doesn't block .env** — Add `.env` to `.gitignore` immediately
   to prevent leaking secrets.
4. **No secrets protection** — Add `permissions.deny` rules to block reading
   `.env` files and secrets directories.

### Quick-win improvement path

Run the following to generate a starter baseline:
```bash
cd C:\Users\naorp\basketstrategy
npx @nerviq/cli setup
```

Estimated score after applying setup: **~45-55/100**
Estimated score after full implementation: **~70-80/100**

---

## Before / After Projection

| Phase | Score | Action |
|-------|-------|--------|
| Current | 10 | No AI config |
| After `nerviq setup` | ~50 | Baseline files generated |
| After manual enhancement | ~75 | Domain-specific rules added |

*Generated by nerviq audit --platform claude --json*
